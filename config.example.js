// Copy this file to config.js and replace 'YOUR_GITHUB_TOKEN' with your Personal Access Token (PAT).
// config.js is ignored by Git, so your token won't be committed.

window.config = {
    githubToken: 'YOUR_GITHUB_TOKEN'
};
