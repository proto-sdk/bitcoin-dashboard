{
  "name": "proto-dashboard-backend",
  "version": "1.0.0",
  "description": "Secure backend proxy for Proto API PR Dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "hash-password": "node -e \"console.log('Password hash:', require('crypto').createHash('sha256').update(process.argv[1]).digest('hex'))\" "
  },
  "keywords": ["github", "dashboard", "proxy", "authentication"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}
